table:
  name: users
  schema: public
array_relationships:
  - name: memberships
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: memberships
          schema: public
